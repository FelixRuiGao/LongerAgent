/**
 * TitleBar: invisible in the redesign. The drag region is handled by
 * App.tsx's `titlebar-drag` div. Traffic lights come from Electron's
 * hiddenInset titleBarStyle. No visible text or controls needed.
 */

export function TitleBar(): JSX.Element {
  return <></>
}
